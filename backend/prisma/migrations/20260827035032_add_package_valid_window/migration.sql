BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ParkingPackages] ADD [ValidFrom] DATE,
[ValidTo] DATE;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
